# REFLECTION.md · 反思报告

## 哪些 Superpowers 技能发挥了最大作用

writing-plans 是最有用的一个。把项目拆成 9 个批次、每个批次 2-7 个 task 之后，每个 task 的目标和边界都很清楚，不会出现"做着做着不知道下一步该做什么"的情况。

test-driven-development 在理论上是对的，但实际做的时候会有点别扭。因为 AI 写代码的习惯是先写实现再补测试，要强制"先写测试让它失败再写实现"需要刻意调整。不过对于护栏这种核心机制，先写测试确实能帮你想清楚边界条件。

## 哪些形式大于实质

brainstorming 阶段对于这个项目来说略显多余。因为"做一个编码 agent 框架"这个方向一开始就很明确，不是那种需要反复揉捏的模糊需求。但如果是做 B 类应用项目，brainstorming 可能会更有用。

## TDD 在 AI 协作下是阻碍还是放大器

对于这个项目，TDD 在治理模块（M3）上是放大器——先写测试明确边界条件（比如"rm -rf / 应该被拦截"），再写实现，逻辑很清晰。但在工具系统（M2）上有点阻碍——那些工具本质上就是封装系统调用，测试更多是验证参数传递是否正确，先写测试反而多了一步。

## subagent 能自主运行多久

这个项目是直接在对话中完成所有代码的，没有分派 subagent。但模块化的设计让每个模块都可以独立测试，如果后续需要分派 subagent 也很容易。

## 什么样的 task 颗粒度最优

按模块（M1-M6）分批，每批 2-7 个文件，这个颗粒度比较合适。每个 task 对应一个文件或一个类，写完就能测试，反馈周期短。

## SPEC / PLAN 质量如何影响实现质量

PLAN 里的依赖关系图（第1批→第2批→第3批→...）对实现顺序帮助很大。如果没有这个依赖图，可能会先做 M5 记忆系统再做 M3 治理，但主循环依赖 LLM 接口，所以必须先做第2批再第3批。

## 最有效的 prompt 策略

"明确边界条件"是最有效的。比如护栏引擎的 prompt 里明确写了哪些命令是 DENY、哪些是 NEED_APPROVAL，代码生成的质量很高。反过来，如果 prompt 只写"做一个安全机制"，AI 容易做出模糊的实现。

## 凭据与分发迫使想清楚的问题

凭据管理这块，如果没有课程要求，我可能就用 .env 文件了。但课程要求用 OS 凭据管理器，就不得不去了解 keytar 的用法，最后做了两层方案（keytar 主 + .env 备）。这确实是更安全的做法——.env 是明文，进程环境可见，存在泄露风险。

分发方面，Docker 多阶段构建是标准做法，但要注意 Docker 容器内 keytar 不可用的问题，所以 README 里明确写了这个限制。

## 如果重做会改变什么

可能会在架构设计阶段就把 mock 测试的接口定义得更完整一些。当前 mock 实现只支持预设响应和查询历史，但如果有更复杂的场景（比如模拟 LLM 调用失败），mock 需要更丰富的配置。

另外，config-loader 的实现比较简单，只做了 JSON 文件加载 + 默认值合并。如果重做，可能会支持 YAML 或更多格式。

## 对 Superpowers 方法论的批判

Superpowers 的核心假设是"AI 需要一套固定的流程才能可靠工作"。这个假设在大部分情况下成立，但有一个问题：流程本身增加了 overhead。对于简单的项目（比如这个 harness 只有 6 个模块），严格按照七步工作流走完每一步，文档产出的时间可能接近实现时间的一半。

另一个假设是"每次都用新鲜 subagent 更好"。但实际开发中，同一个 agent 对代码库的上下文理解会随着对话深入越来越好。频繁切换 agent 可能在简单任务上提高效率，但在需要理解复杂依赖关系的任务上反而降低效率。

总的来说，Superpowers 的流程对于"第一次做的事情"（比如这个项目）很有帮助，但对于"已经做过很多次的事情"可能会有不必要的约束。最好的方式是：在项目前期用 Superpowers 的流程建立框架，在后期根据实际情况灵活调整。